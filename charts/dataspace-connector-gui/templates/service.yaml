apiVersion: v1
kind: Service
metadata:
  name: {{ include "dataspace-connector-gui.fullname" . }}
  labels:
    {{- include "dataspace-connector-gui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dataspace-connector-gui.selectorLabels" . | nindent 4 }}
