apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dataspace-connector-gui.fullname" . }}-test-connection"
  labels:
    {{- include "dataspace-connector-gui.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dataspace-connector-gui.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
