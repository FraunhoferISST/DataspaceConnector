package de.fraunhofer.isst.dataspaceconnector.services.resources;

class BasePathTest {

    /*@Test
    void toString_nothing_correctPath() {
        *//* ARRANGE *//*
        final var input = BasePath.OFFERS;

        *//* ACT *//*
        final var host = Mockito.when(EndpointUtils.getCurrentBasePathString()).thenReturn("https://host");
        final var basePath = input.toString();
        final var path = host + basePath;

        *//* ASSERT *//*
        assertEquals(path, basePath);
    }*/
}
